export type RoutineSuperset = {
  routine_id: string;
  routine_uuid: string;
  exercise_id: string;
  custom_exercise_id: string;
};

export type WorkoutSuperset = {
  workout_id: string;
  workout_uuid: string;
  exercise_id: string;
  custom_exercise_id: string;
};
